# 한끼통살 반응형 웹 구현 — 순차 프롬프트 3단계

프롬프트 제안시 데스크탑 이미지와 모바일 이미지 캡쳐본 올려놓기

**순서: 구조 선언 → 요소별 변화 지정 → 브레이크포인트 명시**

Google Antigravity(또는 Claude Code)에 아래 프롬프트를 **순서대로 하나씩** 입력합니다.
각 단계는 이전 단계에서 생성된 코드를 그대로 이어받아 수정하는 방식이므로, 반드시 순서를 지켜서 진행하세요.

html/css/js로 구현하고 img는 img디렉토리를 제작해서 img 를 연동할 수 있게 만들고 다운로드할 수 있게 해달라고 작성하기
---

## 1단계 · 구조 선언 (Structure)

> 레이아웃의 뼈대만 먼저 잡습니다. 텍스트·색상·이미지는 아직 다루지 않습니다.

```
"한끼통살"이라는 닭가슴살 판매 쇼핑몰의 홈 화면을 HTML/CSS로 만들 거야.
반응형 웹으로 설계할 건데, 지금은 레이아웃 뼈대(스켈레톤)만 잡아줘.
텍스트·이미지·색상 같은 세부 콘텐츠는 다음 단계에서 채울 거니까,
지금은 박스 구조와 배치(display, flex/grid, width)만 정확하게 만들어줘.

[데스크탑 레이아웃 — 480px 초과]
- 좌우 2단 구조
- 좌측 40%: 브랜드 소개 영역 (로고, 타이틀, 서브카피, 검색창, 태그 리스트가 들어갈 자리)
- 우측 60%: 서비스 화면 프리뷰 영역 (헤더, 배너, 캐러셀, 카테고리 카드가 들어갈 자리)
- 페이지 최상단부터 좌측 텍스트 블록이 시작되고, 별도의 상단 고정 헤더는 없음

[모바일 레이아웃 — 480px 이하]
- 단일 컬럼 구조로 전환
- 좌측 브랜드 소개 블록은 제거
- 대신 화면 최상단부터: 상단 헤더(햄버거·로고·검색·장바구니) → 프로모션 배너 →
  카테고리 캐러셀 → 카테고리 아이콘 그리드 순서로 세로 배치
- 화면 최하단에는 고정 하단 탭바 영역을 별도로 잡아줘

HTML은 시맨틱 태그(header, nav, main, section, footer)를 사용하고,
CSS는 별도 파일(style.css)로 분리해서 작성해줘.
```

---

## 2단계 · 요소별 변화 지정 (Element Changes)

> 1단계에서 잡은 뼈대에 실제 요소를 채우면서, 디바이스별로 무엇이 사라지고·이동하고·새로 생기는지 명확히 지정합니다.

```
방금 만든 구조를 기준으로, 아래 규칙에 따라 요소를 채워줘.

[사라지는 요소 — 모바일에서]
- 좌측 브랜드 소개 텍스트, 검색창, 태그 리스트는 모바일 레이아웃에서 완전히 제외해줘
  (숨김 처리가 아니라 애초에 다른 컴포넌트로 대체되는 구조로)

[이동하는 요소]
- 핵심 메뉴(홈 / 건강 / 클리닉 / 프로필)는 데스크탑에는 없다가,
  모바일에서는 화면 하단 고정 탭바로 새로 등장하는 형태로 만들어줘
  (아이콘 + 라벨 4개, 터치 영역 44px 이상)

[새로 생기는 요소 — 모바일에서]
- 프로모션 배너: 오렌지 계열 배경, 할인 문구, 상품 썸네일 이미지 자리
- "어디가 불편한가요?" 아이콘형 카테고리 5개: 관절케어 / 눈물눈병 / 피부보습 / 면역력UP / 구강치석
  (원형 아이콘 + 라벨, 가로 스크롤 또는 5등분 그리드)
- 캐러셀 인디케이터(1/3 형태) + 좌우 화살표 버튼

[공통으로 유지되는 요소 — 데스크탑·모바일 모두]
- "강아지 생애주기별 맞춤 영양 케어" 상품 카드
  (상품 이미지, 타이틀, 서브텍스트, 해시태그, 브랜드 배지 포함)
  → 데스크탑에서는 우측 영역 폭에 맞춰서, 모바일에서는 폭 100%로 채워지도록

색상은 #AB2F05와 #5A413B를 포인트로 쓰고,
배경은 #FFF8F5 으로 채워줘.
```

---

## 3단계 · 브레이크포인트 명시 (Breakpoint)

> 지금까지는 레이아웃과 콘텐츠에 집중했다면, 이번 단계에서 실제 전환 지점을 숫자로 못박고 모바일 퍼스트로 정리합니다.

```
이제 media query를 정리해서 반응형을 완성해줘.

- 기준 브레이크포인트: 480px
  (480px 이하 = 모바일, 481px 이상 = 데스크탑)
- 모바일 퍼스트 방식으로 다시 정리해줘:
  기본 CSS는 모바일 레이아웃을 기준으로 작성하고,
  @media (min-width: 481px) 안에서 데스크탑 전용 스타일(2단 배치, 좌측 브랜드 블록 노출 등)을 추가하는 구조로 바꿔줘
- 하단 탭바는 @media (min-width: 481px)에서 display: none 처리해줘
- 폰트 크기는 clamp()를 사용해서 375px~480px 구간에서 텍스트가 줄바꿈으로 깨지지 않게 보정해줘
  예: font-size: clamp(0.9rem, 2vw + 0.5rem, 1.2rem);
- 이미지가 들어갈 자리는 object-fit: cover로 비율이 깨지지 않게 처리해줘

작업이 끝나면 360px / 375px / 480px / 768px / 1024px 다섯 가지 너비에서
레이아웃이 깨지는 부분이 있는지 각각 확인하고, 문제가 있으면 스스로 수정해줘.
```

---

### 진행 팁
- 각 단계 완료 후 결과물을 눈으로 확인하고, 다음 단계로 넘어가기 전에 "이 구조 맞아, 다음 단계 진행해줘" 정도로 확인 메시지를 넣어주면 AI가 이전 결정을 임의로 바꾸지 않습니다.
- 2단계에서 요소가 과하게 한 번에 들어가 헷갈리면, "데스크탑 카드부터" → "모바일 배너부터" 식으로 더 잘게 쪼개 진행해도 됩니다.
- 3단계 이후 실제 배포 전에는 강의 자료 09챕터의 "배포 전 체크리스트"(터치 영역 44px, 가로 스크롤 여부, 폰트 최소 크기, 이미지 깨짐)를 함께 점검하세요.
